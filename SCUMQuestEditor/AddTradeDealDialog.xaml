<Window x:Class="SCUMQuestEditor.AddTradeDealDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Add Trade Deal" Height="480" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        Foreground="{DynamicResource MaterialDesignBody}">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="TxtSearch" Grid.Row="0" Grid.ColumnSpan="2" 
                 Margin="0,0,0,5" Padding="5" 
                 Style="{StaticResource CompactTextBox}"
                 materialDesign:HintAssist.Hint="Type to search items..."
                 TextChanged="TxtSearch_TextChanged"/>

        <ListBox x:Name="LstItems" Grid.Row="1" Grid.ColumnSpan="2" 
                 Margin="0,0,0,5" 
                 Style="{StaticResource MaterialDesignListBox}"
                 HorizontalContentAlignment="Stretch"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Price:" 
               HorizontalAlignment="Right" VerticalAlignment="Center" 
               Margin="0,0,10,5" Style="{StaticResource CompactLabel}"/>
        <TextBox x:Name="TxtPrice" Grid.Row="2" Grid.Column="1" 
                 Margin="0,0,0,5" Text="50"
                 Style="{StaticResource CompactTextBox}"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Amount:" 
               HorizontalAlignment="Right" VerticalAlignment="Center" 
               Margin="0,0,10,5" Style="{StaticResource CompactLabel}"/>
        <TextBox x:Name="TxtAmount" Grid.Row="3" Grid.Column="1" 
                 Margin="0,0,0,5" Text="1"
                 Style="{StaticResource CompactTextBox}"/>

        <Label Grid.Row="4" Grid.Column="0" Content="Fame Required:" 
               HorizontalAlignment="Right" VerticalAlignment="Center" 
               Margin="0,0,10,5" Style="{StaticResource CompactLabel}"/>
        <TextBox x:Name="TxtFame" Grid.Row="4" Grid.Column="1" 
                 Margin="0,0,0,5" Text="25"
                 Style="{StaticResource CompactTextBox}"/>

        <CheckBox x:Name="ChkAllowExcluded" Grid.Row="5" Grid.ColumnSpan="2" 
                  Content="Allow Excluded" IsChecked="True" 
                  Margin="0,10,0,10" HorizontalAlignment="Left"
                  Style="{StaticResource MaterialDesignCheckBox}"/>

        <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="BtnOk" Content="OK" Width="75" Height="25" 
                    Margin="0,0,5,0" Click="BtnOk_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
            <Button x:Name="BtnCancel" Content="Cancel" Width="75" Height="25" 
                    Click="BtnCancel_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
