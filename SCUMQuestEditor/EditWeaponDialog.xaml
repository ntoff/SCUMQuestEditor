<Window x:Class="SCUMQuestEditor.EditWeaponDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Edit Allowed Weapons" Height="500" Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        Foreground="{DynamicResource MaterialDesignBody}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="TxtSearch" Grid.Row="0" Margin="5,5,5,5" Padding="5" 
                  Style="{StaticResource CompactTextBox}"
                  materialDesign:HintAssist.Hint="Type to search weapons..."
                  TextChanged="TxtSearch_TextChanged"/>

        <ListBox x:Name="LstWeapons" Grid.Row="1" Margin="5"
         SelectionMode="Multiple"
         Style="{StaticResource MaterialDesignListBox}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding}" IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,10,10,10">
            <Button x:Name="BtnOk" Content="OK" Width="75" Height="25" 
                    Margin="0,0,10,0" Click="BtnOk_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
            <Button x:Name="BtnCancel" Content="Cancel" Width="75" Height="25" 
                    Click="BtnCancel_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
