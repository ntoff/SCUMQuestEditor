<Window x:Class="SCUMQuestEditor.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="About" Height="360" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        Foreground="{DynamicResource MaterialDesignBody}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="HeaderPanel" Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Image Width="64" Height="64" Margin="0,0,0,10" x:Name="ImgIcon"/>
            <TextBlock Text="SCUM&#169; Quest Editor" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock x:Name="LblVersion" FontSize="12" Foreground="{DynamicResource MaterialDesignBodyLight}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            <TextBlock x:Name="LblBuildDate" FontSize="12" Foreground="{DynamicResource MaterialDesignBodyLight}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock TextWrapping="Wrap" Text="A tool for creating and editing SCUM&#169; quest files." HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock TextWrapping="Wrap" Text="This program and its creator are not associated with Gamepires." HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock TextWrapping="Wrap" Text="SCUM is copyright Gamepires." HorizontalAlignment="Center" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="OK" Width="75" Height="25" 
                    Click="BtnOk_Click" 
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
